#pragma once

#include <list>
#include <vector>
#include <map>
#include <string>

class AutoDemo
{
public:
	AutoDemo();
	virtual ~AutoDemo();

	void testAuto(std::list<std::vector<std::map<std::string, std::string>>> lstVal);

private:

};

